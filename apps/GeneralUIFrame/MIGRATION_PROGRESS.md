# GeneralUIFrame 迁移进度

本文档用于记录「单页应用 -> GeneralUIFrame 模块」迁移状态。

## 当前状态（2026-03）

- 总体阶段：`进行中`
- 已接入应用：`2`
- 未开始应用：`N`（按后续清单继续补充）

## 已完成

### 1) 壳层与通用能力

- 统一主题与样式体系接入（复用主仓库 `themes + app styles`）
- 统一设置面板（外观/布局/工作区/AI模型设置）
- 设置项本地持久化（`localStorage`）
- 应用壳层状态持久化（首页/当前应用/每应用侧栏与meta折叠）
- Header 应用切换入口与首页返回
- 左右分割条拖拽、面板折叠行为接入

### 2) 应用：账单处理（`billing-workbench`）

- 独立目录完成：`src/apps/billing-workbench`
- 布局迁移完成：前置参数在 Sidebar，预览与调节在 Main
- 处理链路完成：读取 Excel -> 过滤支出 -> 生成结果 -> 下载
- 列宽调节与配置持久化完成
- AI 设置读取入口预留完成（共享设置）

### 3) 应用：路径动画生成器（`path-arrow-editor`）

- 独立目录完成：`src/apps/path-arrow-editor`
- 布局迁移完成：原右侧调节工具迁移到左侧 Sidebar
- 主区接入：画布、缩放、路径编辑、播放
- 路径 JSON 导入导出完成
- 导出能力接入：GIF / MP4(WebM 回退) / PNG
- 应用状态持久化完成（路径、样式、缩放、选中项）

## 待完成（下一批）

### 路径动画生成器补齐项

- 手柄（`handleIn/handleOut`）精细编辑对齐到原单页
- 编辑模式（框选多点 + 对齐工具）完整迁移
- 导出设置弹窗（格式/分辨率/FPS）与进度 UI 对齐
- PNG 序列帧 ZIP 导出能力补齐

### 通用演进

- 抽取跨应用公共组件（Toolbar/Status/Canvas Host）
- 为后续应用建立统一迁移模板与验收清单
